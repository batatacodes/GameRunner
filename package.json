{
  "name": "endless-runner-3d",
  "version": "1.0.0",
  "private": true,
  "description": "Endless Runner 3D (React + Three.js) — leve e otimizado para mobile/PC.",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html --public-url ./"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "three": "^0.156.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}